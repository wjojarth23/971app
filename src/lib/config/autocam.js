// Global quick-disable flag for Autocam
// Set to `true` to prevent any autocam processing from happening
// Useful for turning off autocam during development or when pushing unrelated changes
// Flip to `false` to enable autocam again
export const DISABLE_AUTOCAM = true;
