# Autocam Service Dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
requests>=2.31.0
pydantic>=2.5.0

# PenguinCAM core dependencies
ezdxf>=1.0.0
shapely>=2.0.0

# Optional: Onshape API authentication
# (only needed if using Onshape DXF export)
