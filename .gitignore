# OS
.DS_Store
Thumbs.db

# Leftover build artifacts from before this became a plain static page -
# this repo has no build step now, but keep these ignored in case anything
# ever gets run here again.
node_modules
.svelte-kit
.vite
build

# Env / secrets - never commit these regardless of what the site is
.env
.env.*
.mcp.json
secrets.txt

# Local Claude Code config
.claude/
